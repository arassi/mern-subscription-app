let bcrypt;_3e5‍.x([["hashPassword",()=>hashPassword],["comparePassword",()=>comparePassword]]);_3e5‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);

// When a user registers hash is password
       const hashPassword = (password) => {
    return new Promise((resolve, reject) => {
        bcrypt.genSalt(12, (err, salt) => {
            if(err){
                reject(err);
            }
            bcrypt.hash(password, salt, (err, hash) => {
                if(err){
                    reject(err)
                }
                resolve(hash);
            });
        });
    });
};

// When a user logs in check his password and hash
       const comparePassword = (password, hashed) => {
    return bcrypt.compare(password, hashed);
}